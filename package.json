{
  "name": "ecommerce-web-application-using-django-and-react",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "test": "cd frontend && npm test"
  }
}